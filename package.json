{
  "name": "public_ip",
  "version": "1.0.0",
  "description": "Get public IP address and remote (client) port",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
